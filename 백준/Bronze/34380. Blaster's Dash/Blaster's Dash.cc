#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int m, v;

    int n;
    int k;

    cin >> m >> v;

    cin >> n;
    cin >> k;

    cout << 2 * (20 + n) << '\n';
    return 0;
}