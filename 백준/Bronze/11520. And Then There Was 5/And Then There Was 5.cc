#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	int N;

	cin >> N;


	for (int test_case = 1; test_case <= N; test_case++) {
		int P, D;

		cin >> P >> D;
		cout << D << ' ' << 5 << '\n';
	}



	return 0;
}